{
  "name": "energy-game",
  "version": "1.0.0",
  "description": "an energy game",
  "main": "dist/electron/main.js",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "clean": "rm -r -fo dist",
    "clean:web": "rm -r -fo dist/web",
    "dev:client": "vite",
    "copy:assets": "cp static dist/web -r -Force",
    "build:client": "pnpm run clean:web && tsc -p tsconfig.json && vite build && pnpm run copy:assets",
    "open:client": "vite preview --open",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "electron": "tsc -p electron/tsconfig.json && electron dist/electron/main.js",
    "electron:rebuild": "electron-rebuild -f -w ."
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.1",
    "eslint": "^10.7.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.64.0",
    "vite": "^8.1.4",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^3.2.4"
  },
  "private": true,
  "dependencies": {
    "electron": "^43.1.1"
  }
}
